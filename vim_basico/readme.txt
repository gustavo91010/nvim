
- Para zipar eles:
zip -r vim_basico_vm.zip .vim .vimrc

Fazer o link deles la na home
ln -s /caminho/para/.vim ~/.vim && ln -s /caminho/para/.vimrc ~/.vimrc

quando for comecar uma nova pasta, da o PlugInstall que ele baixa novamente os plugins

